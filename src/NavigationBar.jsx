const NavigationBar = () => {
    return(
        <nav className = "navigationBar">
            <h1>
                Navigation Bar
            </h1>
        </nav>
    );
}

export default NavigationBar;